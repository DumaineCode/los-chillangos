# Dependencies & build output (rebuilt inside the image)
node_modules
.next
.turbo
out
build

# Local media uploads (prod uses R2 / external volume)
media

# Env & secrets — provided by Dokploy at runtime, never baked in
.env
.env.*
!.env.example

# VCS & tooling
.git
.gitignore
.github
.vscode
.idea

# Tests & coverage
coverage
playwright-report
test-results
e2e

# Misc
Dockerfile
.dockerignore
README.md
*.log
.DS_Store
