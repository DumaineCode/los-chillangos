node_modules
.next
out
coverage
pnpm-lock.yaml
package-lock.json

# Ported verbatim from legacy styles.css in PR 4 — do not reformat to preserve
# fidelity. Tailwind refactor is a future SDD.
app/globals.css

# Test artifacts
playwright-report
test-results
playwright/.cache

# Misc
.atl

# Payload-generated (rebuilt on `pnpm generate:types` / `generate:importmap`)
src/payload-types.ts
app/(payload)/admin/importMap.js
media
